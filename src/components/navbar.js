import React, {Component} from 'react';

const Navbar = (props) => {
  return (
      <nav className="navbar">
        <a href="/" className="navbar-brand">YouTube Search</a>
      </nav>
  );
}
export default Navbar;
